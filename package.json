{
  "name": "midi-ripples",
  "version": "1.0.0",
  "description": "WebGPU MIDI visualizer — play notes and watch ripples of light respond in real time",
  "main": "dist-electron/main/index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && electron-builder",
    "preview": "vite preview"
  },
  "dependencies": {
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "electron": "^29.0.0",
    "electron-builder": "^24.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-electron": "^0.28.0",
    "vite-plugin-electron-renderer": "^0.14.0"
  }
}
